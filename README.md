# gentact_descriptions
URDF builder for GenTact skin units!

# Creating a skin unit

Each individual skin sensor is defined by a xacro file. For a custom skin, create a xacro file under `robots/skin/<type>/<unit name>`. It should include the base stl file used, as well as a description of each sensor.